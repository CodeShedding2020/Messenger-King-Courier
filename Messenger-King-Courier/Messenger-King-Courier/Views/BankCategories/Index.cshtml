@model IEnumerable<Messenger_King_Courier.Models.AppModels.BankCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bank_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bank_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BankCat_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BankCat_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BankCat_ID })
        </td>
    </tr>
}

</table>
