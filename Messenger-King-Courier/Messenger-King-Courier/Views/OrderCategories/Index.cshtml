@model IEnumerable<Messenger_King_Courier.Models.AppModels.OrderCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderCat_Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderCat_Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderCat_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderCat_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderCat_ID })
        </td>
    </tr>
}

</table>
