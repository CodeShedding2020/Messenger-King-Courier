@model IEnumerable<Messenger_King_Courier.Models.AppModels.ClientCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientCat_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientCat_Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientCat_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientCat_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientCat_ID })
        </td>
    </tr>
}

</table>
